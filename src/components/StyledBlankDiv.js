import styled from 'styled-components'

const StyledBlankDiv = styled.div`
  height: ${props => props.height};
  background: rgba(0, 0, 255, 0.3);
  padding: 1.2rem;
  font-size: 2.4rem;
  font-weight: bolder;
  color: #fff;
`

export default StyledBlankDiv
